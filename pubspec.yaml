name: ebike_app
description: Smart E-Bike sharing app for Konza Technopolis
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # ✅ Corrected
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1

  mobile_scanner: ^3.5.2
  qr_flutter: ^4.1.0

  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  uuid: ^4.2.2
  http: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
